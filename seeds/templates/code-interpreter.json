{
  "alias": "code-interpreter",
  "vcpu": 2,
  "memory_mib": 2048,
  "disk_mib": 8192,
  "build_spec": {
    "from": "python:3.11",
    "steps": [
      { "run": "apt-get update && apt-get install -y --no-install-recommends git curl ca-certificates build-essential libfreetype6-dev libpng-dev && rm -rf /var/lib/apt/lists/*" },
      { "run": "pip install --no-cache-dir numpy pandas scikit-learn matplotlib jupyter ipython requests pillow seaborn plotly openpyxl" },
      { "workdir": "/workspace" }
    ]
  }
}
