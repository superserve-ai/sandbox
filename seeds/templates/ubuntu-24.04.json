{
  "alias": "ubuntu-24.04",
  "vcpu": 1,
  "memory_mib": 1024,
  "disk_mib": 4096,
  "build_spec": {
    "from": "ubuntu:24.04",
    "steps": [
      { "run": "apt-get update && apt-get install -y --no-install-recommends git curl ca-certificates vim build-essential python3 python3-pip && rm -rf /var/lib/apt/lists/*" },
      { "workdir": "/workspace" }
    ]
  }
}
