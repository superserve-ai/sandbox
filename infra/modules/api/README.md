# api

Terraform skeleton for Cloud Run API services.

Known existing service shape from inventory:

- service `superserve-api`
- region `us-central1`
- VPC connector reference `ss-vpc-conn-f1b3552`
- VMD upstream `10.0.0.2:50051`

The module currently captures only the deployment contract.
