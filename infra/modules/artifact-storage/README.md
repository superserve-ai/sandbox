# artifact-storage

Terraform skeleton for Artifact Registry and Cloud Storage.

Known existing inventory to model later:

- bucket `superserve-artifacts`
- Docker image references under `us-central1-docker.pkg.dev`

This module is intentionally provider-free for now.
