# iam

Terraform skeleton for service accounts and IAM bindings.

Expected scope:

- Superserve API runtime accounts
- CI/CD service accounts
- later imports of pre-existing Superserve-specific IAM identities

No provider resources are defined yet; this is a schema-only module skeleton.
