# sandbox-host

Terraform skeleton for VMD or other sandbox VM hosts.

Known existing host to model later:

- `superserve-vmd-staging`
- internal IP `10.0.0.2`
- tag `superserve-vmd`

This module only defines the host contract for now.
