{
  "alias": "superserve/python-ml",
  "vcpu": 2,
  "memory_mib": 2048,
  "disk_mib": 4096,
  "build_spec": {
    "from": "python:3.11",
    "steps": [
      {
        "run": "apt-get update && apt-get install -y --no-install-recommends git curl build-essential ca-certificates && rm -rf /var/lib/apt/lists/*"
      },
      {
        "run": "pip install --no-cache-dir numpy pandas scikit-learn matplotlib jupyter ipython requests"
      }
    ]
  }
}