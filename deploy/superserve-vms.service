[Unit]
Description=Delegated cgroup subtree for direct-spawn VMs
# Ordered before vmd so the delegated subtree exists when vmd validates and
# adopts it at startup. Placed under sandboxes.slice so direct-spawn VMs share
# the one aggregate memory ceiling with the legacy firecracker@ units.
Before=superserve-vmd.service

[Service]
# A minimal keeper process owns the delegated subtree: it holds the cgroup so
# systemd does not GC it across zero-VM windows, and gives the subtree a stable
# lifetime handle. vmd moves it into keeper/ at bootstrap, then enables
# controllers on the freed root. Restart is disabled deliberately — a restart
# would try to re-enter the controller-enabled inner root and fail (cgroup v2
# no-internal-process rule); the keeper is not a VM-survival dependency, so
# vmd handles its death (block new launches, existing VMs keep running).
Type=exec
ExecStart=/usr/bin/sleep infinity
Slice=sandboxes.slice
Delegate=yes
KillMode=process
Restart=no
# Every direct-spawn VM's processes and THREADS share this one service's task
# ceiling (per-unit VMs never did). Left unset, systemd's TasksMax default —
# 15% of kernel.threads-max, host-size dependent — becomes an invisible fleet
# cap that binds as clone3 EAGAIN at create. Fleet task containment is a
# deliberate slice/host decision, not a service default.
TasksMax=infinity
# The slice's CPU/IO accounting enables weight-based sharing among its
# children, where every legacy firecracker@ unit is one peer and this service
# — holding the ENTIRE direct population — is also one peer at the default
# weight 100. At host saturation during mixed-mode migration that starves the
# direct fleet to a single legacy VM's share. The maximum weight makes this
# service weigh as much as 100 legacy peers — a static bound that re-inverts
# only past 100 simultaneously busy legacy VMs; if mixed-mode ever needs
# true proportionality, the reconciler knows both populations each pass and
# can set-property the weight dynamically. Per-VM fairness inside the
# subtree stays with the kernel scheduler (see the slice's CPUQuota note).
CPUWeight=10000
IOWeight=10000

[Install]
WantedBy=multi-user.target
