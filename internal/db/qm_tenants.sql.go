// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: qm_tenants.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createQMTenant = `-- name: CreateQMTenant :one
INSERT INTO qm.tenants (
    team_id, slug, org_name, admin_email, sign_in, model_provider, harness, created_by
)
VALUES (
    $1, $2, $3, $4, $5, $6, COALESCE($7::text, 'pi'), $8
)
RETURNING id, team_id, slug, org_name, admin_email, sign_in, model_provider, harness, status, public_url, image_tag, cloud_run_service, db_name, bucket_name, service_account, sandbox_api_key_id, created_by, created_at, updated_at
`

type CreateQMTenantParams struct {
	TeamID        uuid.UUID   `json:"team_id"`
	Slug          string      `json:"slug"`
	OrgName       string      `json:"org_name"`
	AdminEmail    string      `json:"admin_email"`
	SignIn        string      `json:"sign_in"`
	ModelProvider string      `json:"model_provider"`
	Harness       *string     `json:"harness"`
	CreatedBy     pgtype.UUID `json:"created_by"`
}

func (q *Queries) CreateQMTenant(ctx context.Context, arg CreateQMTenantParams) (QmTenant, error) {
	row := q.db.QueryRow(ctx, createQMTenant,
		arg.TeamID,
		arg.Slug,
		arg.OrgName,
		arg.AdminEmail,
		arg.SignIn,
		arg.ModelProvider,
		arg.Harness,
		arg.CreatedBy,
	)
	var i QmTenant
	err := row.Scan(
		&i.ID,
		&i.TeamID,
		&i.Slug,
		&i.OrgName,
		&i.AdminEmail,
		&i.SignIn,
		&i.ModelProvider,
		&i.Harness,
		&i.Status,
		&i.PublicUrl,
		&i.ImageTag,
		&i.CloudRunService,
		&i.DbName,
		&i.BucketName,
		&i.ServiceAccount,
		&i.SandboxApiKeyID,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteQMTenantSecretRef = `-- name: DeleteQMTenantSecretRef :exec
DELETE FROM qm.tenant_secrets
WHERE tenant_id = $1 AND name = $2
`

type DeleteQMTenantSecretRefParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	Name     string    `json:"name"`
}

func (q *Queries) DeleteQMTenantSecretRef(ctx context.Context, arg DeleteQMTenantSecretRefParams) error {
	_, err := q.db.Exec(ctx, deleteQMTenantSecretRef, arg.TenantID, arg.Name)
	return err
}

const getQMTenant = `-- name: GetQMTenant :one
SELECT id, team_id, slug, org_name, admin_email, sign_in, model_provider, harness, status, public_url, image_tag, cloud_run_service, db_name, bucket_name, service_account, sandbox_api_key_id, created_by, created_at, updated_at FROM qm.tenants
WHERE id = $1 AND team_id = $2
`

type GetQMTenantParams struct {
	ID     uuid.UUID `json:"id"`
	TeamID uuid.UUID `json:"team_id"`
}

func (q *Queries) GetQMTenant(ctx context.Context, arg GetQMTenantParams) (QmTenant, error) {
	row := q.db.QueryRow(ctx, getQMTenant, arg.ID, arg.TeamID)
	var i QmTenant
	err := row.Scan(
		&i.ID,
		&i.TeamID,
		&i.Slug,
		&i.OrgName,
		&i.AdminEmail,
		&i.SignIn,
		&i.ModelProvider,
		&i.Harness,
		&i.Status,
		&i.PublicUrl,
		&i.ImageTag,
		&i.CloudRunService,
		&i.DbName,
		&i.BucketName,
		&i.ServiceAccount,
		&i.SandboxApiKeyID,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getQMTenantBySlug = `-- name: GetQMTenantBySlug :one
SELECT id, team_id, slug, org_name, admin_email, sign_in, model_provider, harness, status, public_url, image_tag, cloud_run_service, db_name, bucket_name, service_account, sandbox_api_key_id, created_by, created_at, updated_at FROM qm.tenants
WHERE slug = $1 AND team_id = $2
`

type GetQMTenantBySlugParams struct {
	Slug   string    `json:"slug"`
	TeamID uuid.UUID `json:"team_id"`
}

func (q *Queries) GetQMTenantBySlug(ctx context.Context, arg GetQMTenantBySlugParams) (QmTenant, error) {
	row := q.db.QueryRow(ctx, getQMTenantBySlug, arg.Slug, arg.TeamID)
	var i QmTenant
	err := row.Scan(
		&i.ID,
		&i.TeamID,
		&i.Slug,
		&i.OrgName,
		&i.AdminEmail,
		&i.SignIn,
		&i.ModelProvider,
		&i.Harness,
		&i.Status,
		&i.PublicUrl,
		&i.ImageTag,
		&i.CloudRunService,
		&i.DbName,
		&i.BucketName,
		&i.ServiceAccount,
		&i.SandboxApiKeyID,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getQMTenantSecretRef = `-- name: GetQMTenantSecretRef :one
SELECT secret_ref FROM qm.tenant_secrets
WHERE tenant_id = $1 AND name = $2
`

type GetQMTenantSecretRefParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	Name     string    `json:"name"`
}

func (q *Queries) GetQMTenantSecretRef(ctx context.Context, arg GetQMTenantSecretRefParams) (string, error) {
	row := q.db.QueryRow(ctx, getQMTenantSecretRef, arg.TenantID, arg.Name)
	var secret_ref string
	err := row.Scan(&secret_ref)
	return secret_ref, err
}

const isQMSlugAvailable = `-- name: IsQMSlugAvailable :one
SELECT qm.slug_available($1)::boolean AS available
`

// Answers across every team (see qm.slug_available); the UNIQUE constraint
// on slug remains the authoritative guard at insert time.
func (q *Queries) IsQMSlugAvailable(ctx context.Context, candidate string) (bool, error) {
	row := q.db.QueryRow(ctx, isQMSlugAvailable, candidate)
	var available bool
	err := row.Scan(&available)
	return available, err
}

const listQMTenantSecretRefs = `-- name: ListQMTenantSecretRefs :many
SELECT tenant_id, name, secret_ref FROM qm.tenant_secrets
WHERE tenant_id = $1
ORDER BY name
`

func (q *Queries) ListQMTenantSecretRefs(ctx context.Context, tenantID uuid.UUID) ([]QmTenantSecret, error) {
	rows, err := q.db.Query(ctx, listQMTenantSecretRefs, tenantID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []QmTenantSecret{}
	for rows.Next() {
		var i QmTenantSecret
		if err := rows.Scan(&i.TenantID, &i.Name, &i.SecretRef); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listQMTenantsByTeam = `-- name: ListQMTenantsByTeam :many
SELECT id, team_id, slug, org_name, admin_email, sign_in, model_provider, harness, status, public_url, image_tag, cloud_run_service, db_name, bucket_name, service_account, sandbox_api_key_id, created_by, created_at, updated_at FROM qm.tenants
WHERE team_id = $1 AND status <> 'deleted'
ORDER BY created_at DESC
`

func (q *Queries) ListQMTenantsByTeam(ctx context.Context, teamID uuid.UUID) ([]QmTenant, error) {
	rows, err := q.db.Query(ctx, listQMTenantsByTeam, teamID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []QmTenant{}
	for rows.Next() {
		var i QmTenant
		if err := rows.Scan(
			&i.ID,
			&i.TeamID,
			&i.Slug,
			&i.OrgName,
			&i.AdminEmail,
			&i.SignIn,
			&i.ModelProvider,
			&i.Harness,
			&i.Status,
			&i.PublicUrl,
			&i.ImageTag,
			&i.CloudRunService,
			&i.DbName,
			&i.BucketName,
			&i.ServiceAccount,
			&i.SandboxApiKeyID,
			&i.CreatedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setQMTeamScope = `-- name: SetQMTeamScope :exec
SELECT set_config('qm.team_id', $1::text, true)
`

// Declares the team every qm.* query in this transaction is scoped to.
// qm_api's row-level policies key on this setting; the third argument makes
// it transaction-local so it cannot leak across pooled connections.
func (q *Queries) SetQMTeamScope(ctx context.Context, teamID string) error {
	_, err := q.db.Exec(ctx, setQMTeamScope, teamID)
	return err
}

const setQMTenantSecretRef = `-- name: SetQMTenantSecretRef :exec
INSERT INTO qm.tenant_secrets (tenant_id, name, secret_ref)
VALUES ($1, $2, $3)
ON CONFLICT (tenant_id, name) DO UPDATE SET secret_ref = EXCLUDED.secret_ref
`

type SetQMTenantSecretRefParams struct {
	TenantID  uuid.UUID `json:"tenant_id"`
	Name      string    `json:"name"`
	SecretRef string    `json:"secret_ref"`
}

func (q *Queries) SetQMTenantSecretRef(ctx context.Context, arg SetQMTenantSecretRefParams) error {
	_, err := q.db.Exec(ctx, setQMTenantSecretRef, arg.TenantID, arg.Name, arg.SecretRef)
	return err
}

const softDeleteQMTenant = `-- name: SoftDeleteQMTenant :one
UPDATE qm.tenants
SET status = 'deleted', updated_at = now()
WHERE id = $1 AND team_id = $2 AND status <> 'deleted'
RETURNING id, team_id, slug, org_name, admin_email, sign_in, model_provider, harness, status, public_url, image_tag, cloud_run_service, db_name, bucket_name, service_account, sandbox_api_key_id, created_by, created_at, updated_at
`

type SoftDeleteQMTenantParams struct {
	ID     uuid.UUID `json:"id"`
	TeamID uuid.UUID `json:"team_id"`
}

func (q *Queries) SoftDeleteQMTenant(ctx context.Context, arg SoftDeleteQMTenantParams) (QmTenant, error) {
	row := q.db.QueryRow(ctx, softDeleteQMTenant, arg.ID, arg.TeamID)
	var i QmTenant
	err := row.Scan(
		&i.ID,
		&i.TeamID,
		&i.Slug,
		&i.OrgName,
		&i.AdminEmail,
		&i.SignIn,
		&i.ModelProvider,
		&i.Harness,
		&i.Status,
		&i.PublicUrl,
		&i.ImageTag,
		&i.CloudRunService,
		&i.DbName,
		&i.BucketName,
		&i.ServiceAccount,
		&i.SandboxApiKeyID,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateQMTenantResources = `-- name: UpdateQMTenantResources :one
UPDATE qm.tenants
SET public_url         = COALESCE($3, public_url),
    image_tag          = COALESCE($4, image_tag),
    cloud_run_service  = COALESCE($5, cloud_run_service),
    db_name            = COALESCE($6, db_name),
    bucket_name        = COALESCE($7, bucket_name),
    service_account    = COALESCE($8, service_account),
    sandbox_api_key_id = COALESCE($9, sandbox_api_key_id),
    updated_at         = now()
WHERE id = $1 AND team_id = $2 AND status <> 'deleted'
RETURNING id, team_id, slug, org_name, admin_email, sign_in, model_provider, harness, status, public_url, image_tag, cloud_run_service, db_name, bucket_name, service_account, sandbox_api_key_id, created_by, created_at, updated_at
`

type UpdateQMTenantResourcesParams struct {
	ID              uuid.UUID   `json:"id"`
	TeamID          uuid.UUID   `json:"team_id"`
	PublicUrl       *string     `json:"public_url"`
	ImageTag        *string     `json:"image_tag"`
	CloudRunService *string     `json:"cloud_run_service"`
	DbName          *string     `json:"db_name"`
	BucketName      *string     `json:"bucket_name"`
	ServiceAccount  *string     `json:"service_account"`
	SandboxApiKeyID pgtype.UUID `json:"sandbox_api_key_id"`
}

// Records what provisioning created. Every field is optional so each
// provisioning step can persist only what it produced.
func (q *Queries) UpdateQMTenantResources(ctx context.Context, arg UpdateQMTenantResourcesParams) (QmTenant, error) {
	row := q.db.QueryRow(ctx, updateQMTenantResources,
		arg.ID,
		arg.TeamID,
		arg.PublicUrl,
		arg.ImageTag,
		arg.CloudRunService,
		arg.DbName,
		arg.BucketName,
		arg.ServiceAccount,
		arg.SandboxApiKeyID,
	)
	var i QmTenant
	err := row.Scan(
		&i.ID,
		&i.TeamID,
		&i.Slug,
		&i.OrgName,
		&i.AdminEmail,
		&i.SignIn,
		&i.ModelProvider,
		&i.Harness,
		&i.Status,
		&i.PublicUrl,
		&i.ImageTag,
		&i.CloudRunService,
		&i.DbName,
		&i.BucketName,
		&i.ServiceAccount,
		&i.SandboxApiKeyID,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateQMTenantStatus = `-- name: UpdateQMTenantStatus :one
UPDATE qm.tenants
SET status = $3, updated_at = now()
WHERE id = $1 AND team_id = $2 AND status <> 'deleted'
RETURNING id, team_id, slug, org_name, admin_email, sign_in, model_provider, harness, status, public_url, image_tag, cloud_run_service, db_name, bucket_name, service_account, sandbox_api_key_id, created_by, created_at, updated_at
`

type UpdateQMTenantStatusParams struct {
	ID     uuid.UUID `json:"id"`
	TeamID uuid.UUID `json:"team_id"`
	Status string    `json:"status"`
}

// deleted is terminal: a racing worker must not resurrect a retired tenant.
func (q *Queries) UpdateQMTenantStatus(ctx context.Context, arg UpdateQMTenantStatusParams) (QmTenant, error) {
	row := q.db.QueryRow(ctx, updateQMTenantStatus, arg.ID, arg.TeamID, arg.Status)
	var i QmTenant
	err := row.Scan(
		&i.ID,
		&i.TeamID,
		&i.Slug,
		&i.OrgName,
		&i.AdminEmail,
		&i.SignIn,
		&i.ModelProvider,
		&i.Harness,
		&i.Status,
		&i.PublicUrl,
		&i.ImageTag,
		&i.CloudRunService,
		&i.DbName,
		&i.BucketName,
		&i.ServiceAccount,
		&i.SandboxApiKeyID,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
